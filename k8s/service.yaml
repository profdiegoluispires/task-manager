apiVersion: v1
kind: Service
metadata:
  name: task-manager
spec:
  type: NodePort
  selector:
    app: task-manager
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080
